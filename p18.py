import time
class Solution:
    def fourSum(self, nums: list[int], target: int) -> list[list[int]]:
        results = []
        nums.sort()
        for i, w in enumerate(nums):
            t = target
        return results
    
    def test(self):
        return self.fourSum([1,0,-1,0,-2,2], 0)


def main():
    print(Solution().test())
